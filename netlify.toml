[build]
  # Provide a valid no-op command so Netlify doesn't fail with exit code 2
  command = "echo 'Static site ready'"
  publish = "."

[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "public, max-age=0, must-revalidate"
